---
global:
  scrape_interval: 5s
  evaluation_interval: 5s

alerting:

rule_files:

scrape_configs:
  - job_name: 'payment-service-local'
    static_configs:
      - targets: ["host.docker.internal:8081"]